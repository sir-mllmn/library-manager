spring:
  application:
    name: search-statistic-app
  cloud:
    config:
      discovery:
        service-id: config-server-app
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://peer1:8020/eureka/,http://peer2:8021/eureka