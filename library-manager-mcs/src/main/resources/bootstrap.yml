spring:
  application:
    name: library-manager-app
  cloud:
    config:
      discovery:
        service-id: config-server-app
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8020/eureka